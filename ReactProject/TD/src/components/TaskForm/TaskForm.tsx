import React from 'react';
import './TaskForm.scss';

const TaskForm: React.FC = () => {
    return (
        <form className="task-form">
            <h2>Создать задачу</h2>
            {/* Поля формы для создания задачи */}
        </form>
    );
};

export default TaskForm;
